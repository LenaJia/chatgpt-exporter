# PR #1 review and continuation checkpoint

Reviewed base: `a278c31d62d24cd0970965048813df701c0f2905`.
Scope: make the bounded Work exporter buildable and ready for supervised browser testing. No merge, guard changes, live conversation mutation or transcript registration.

## Findings resolved

| Finding in reviewed code | Resolution |
| --- | --- |
| Newest compatible ancestor marker silently selected | Explicit numbered selection (including manual recovery); final interval preview; bind confirmed sessions to Work conversation/first node; reject divergent previous end |
| Retroactive marker asserted current Work as parent and historical message time as marking time | Parent stays null; separate marker basis/conversation; actual marker-creation timestamp |
| Truncated deterministic IDs could collide | UUID session identity, preserved across re-exports |
| Storage could fall back to volatile memory and evict old markers | Strict persistent GM storage, read-back, no eviction; invalid legacy/corrupt data never silently promoted/overwritten |
| Whole conversation spread retained unbounded top-level data | Explicit projection allowlist; derived/synthetic/attachment/archive status recorded |
| Download dispatch treated as export completion | Only download-request metadata; no archived acknowledgement or cursor advancement |
| Fetch/path failures outside handler; navigation could change target | Single action error boundary, busy guard, saved-URL requirement and navigation recheck; no latest-history fallback |
| Smoke test not wired to a command; dist stale | `npm run test:work`, three suites; regenerated dist |
| Test install shared upstream identity | Separate test name/namespace/version; no automatic updates; rollback instructions |

## Verified locally on 2026-09-17

- `npm run test`: pass (source TypeScript check).
- `npm run test:work`: pass (graph/provenance/identity, storage, mocked button flows).
- `./node_modules/.bin/eslint src tests vite.config.ts`: pass.
- `npm run build`: pass (Vite 5.3.2, 477 transformed modules).
- `git diff --check`: pass.
- Guard file unchanged relative to reviewed base.
- Built userscript SHA-256: `fd7b5c7634e57065cf638364afba4dcaf41f13be83752e9e231c7049e0cd05f6`.

Validation used the existing installed dependency tree in a separate worktree, without upgrading dependencies or altering the lockfile. The installed pnpm wrapper rejected a lockfile-version mismatch/automatic install attempt; validation used npm script dispatch and the existing local binaries instead. A fresh locked dependency installation has not been verified this turn.

## Still unverified — do not overclaim

- Actual browser/Tampermonkey installation, menu mounting and GM persistence.
- Live ChatGPT Work URL/API shape and backend current-path correspondence to the visible branch.
- The first excursion's real phrase match, downloaded bytes and attachment coverage.
- Revised v2 projection's actual Root Cellar parser/registry round trip (earlier v1 evidence is not sufficient).
- Concurrent tabs are not transactionally coordinated; retain the single-active-surface rule.
- Ancestry projection excludes off-path content; full execution-trace completeness is not promised.

Next: follow `WORK_SEGMENT_EXPORT.md` browser checklist, inspect the real export, then verify Root Cellar ingestion separately. The Work excursion remains active; no HANDOFF_OUT, return_ready, reintegration or Git merge is implied by this candidate build.
